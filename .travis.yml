---

sudo: required
dist: trusty
language: python
python: "2.7"
branches:
  only:
    - dev


services:
  - docker


before_install:
  - sudo apt-get update -qq
  - sudo docker pull saptadip/raspbian_base
  - sudo docker run saptadip/raspbian_base /bin/bash -c "git clone https://github.com/saptadip/ansible.git /tmp/testRepo && ansible-playbook /tmp/testRepo/custom-setup.yml --syntax-check"
